# Programa conversão Dolar para real

cotacao = float(input("Digite a cotação do dolar hoje: \n"))
dolar = float(input("Digite o valor a ser convertido de dolar para real: \n"))
real = cotacao*dolar

print(f"O valor inserido em dolares convertido para BRL é de R${real}")
